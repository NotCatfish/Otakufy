export const metadata = {
  title: "Global Leaderboard",
  description: "See the top Japanese learners ranked by Satori points and active daily study streaks.",
};

export default function LeaderboardLayout({ children }) {
  return children;
}
