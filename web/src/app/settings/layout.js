export const metadata = {
  title: "Study Settings & Preferences",
  description: "Customize your daily card limits, SRS intervals, audio pronunciation, and theme settings.",
};

export default function SettingsLayout({ children }) {
  return children;
}
