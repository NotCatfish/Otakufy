export const metadata = {
  title: "Japanese-English JLPT Dictionary",
  description: "Search thousands of Japanese Kanji, vocabulary words, and grammar points across JLPT N5 to N1 levels.",
};

export default function DictionaryLayout({ children }) {
  return children;
}
