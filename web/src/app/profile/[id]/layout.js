export const metadata = {
  title: "Learner Profile",
  description: "View community achievements, streak history, and badges for Otakufy learners.",
};

export default function ProfileIdLayout({ children }) {
  return children;
}
